{
  "frontend/src/index.css": "Global styles with OKLCH teal/slate-green color system for light and dark modes, custom font imports (Inter + Plus Jakarta Sans), and scrollbar utilities.",
  "frontend/tailwind.config.js": "Extended Tailwind config with teal/slate-green palette, Inter/Plus Jakarta Sans fonts, custom border radii, card shadows, and typing animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "Fully localStorage-based React Query hooks for tasks (`LocalTask`), timetable slots (`LocalSlot`), and day tracker entries (`LocalDayEntry`), all namespaced by `userId` from UserContext (e.g. `acadmind_tasks_<userId>`), with complete CRUD operations and query invalidation.",
  "frontend/src/utils/mockOcr.ts": "Client-side mock OCR utility with `timeStringToMinutes` updated to return a plain `number` (instead of `bigint`) to match the new localStorage-based `LocalSlot` type, and `mockExtractTimetable` for simulating timetable extraction.",
  "frontend/src/utils/mockAi.ts": "Client-side mock AI response generator that produces context-aware replies referencing stored timetable slots and tasks.",
  "frontend/src/App.tsx": "Main app component updated to import the Guide page and register a /guide route in the TanStack Router configuration alongside all existing routes.",
  "frontend/src/components/Layout.tsx": "Shell layout with collapsible sidebar, mobile hamburger header (now including ThemeToggle on the right), a desktop absolute-positioned ThemeToggle in the top-right corner, main content area, and updated footer with caffeine.ai attribution.",
  "frontend/src/components/Sidebar.tsx": "Sidebar navigation updated to include a user profile section at the bottom showing the current user's name/email and a button to open the `UserProfileModal` for profile setup or sign-out.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page updated to use localStorage-based `LocalSlot` and `LocalTask` hooks; shows per-user data, displays a setup banner when no user is configured, and gracefully handles empty states for schedule and tasks sections.",
  "frontend/src/pages/Chat.tsx": "AI chat page that calls Gemini 2.5 Flash directly via `callGeminiApi` (no ICP canister calls for chat), stores all messages in local React state only, shows an amber alert when no API key is present, and maintains full conversation history, typing indicator, clear chat, and Enter-to-send functionality.",
  "frontend/src/components/TimetableSlotModal.tsx": "Reusable dialog for adding/editing timetable slots with subject, day, time, duration, and location fields; updated to work with plain number `startTime`/`durationMinutes` (no bigint) matching the new `LocalSlot` type.",
  "frontend/src/components/TaskModal.tsx": "Reusable dialog for adding/editing tasks with title, due date, and description fields; includes inline validation, clears errors on input change, and resets `saving` state on open.",
  "frontend/src/pages/TimetableManual.tsx": "Weekly timetable page using localStorage-based `LocalSlot` hooks with user-specific namespacing; shows an empty state when no user is set, supports add/edit/delete with toast feedback.",
  "frontend/src/pages/TimetableUpload.tsx": "OCR upload page with drag-and-drop image upload, preview, and extract button; updated `handleSave` to pass plain `number` values for `startTime` and `durationMinutes` (removing the `BigInt()` wrapper) to match the new `useCreateSlot` signature.",
  "frontend/src/pages/Assignments.tsx": "Assignments tracker page using localStorage-based `LocalTask` hooks with user-specific namespacing; shows an empty state when no user is set, supports add/edit/delete/complete with toast feedback and inline loading states.",
  "frontend/src/lib/utils.ts": "Utility function cn() combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/DayTracker.tsx": "Day Tracker page with a new four-card StatsPanel (All Days Hours, This Week Hours, Today Hours, Today Minutes) placed below the header and above the week navigator; all stats are computed reactively from the full entries array using date-fns for week/today filtering, with the rest of the page (week navigator, entry list, daily summary, modals) unchanged.",
  "frontend/src/contexts/UserContext.tsx": "Extended UserContext that manages both the Gemini API key and a `userId` (name/email) for namespacing per-user localStorage data, exposing `updateUserId` and `clearUserId` alongside the existing API key helpers.",
  "frontend/src/utils/geminiChat.ts": "Utility that builds a dynamic system prompt from the user's `LocalSlot` and `LocalTask` data (localStorage-based types), then calls the Gemini 2.5 Flash REST API with conversation history and returns the AI response text.",
  "frontend/public/manifest.json": "Web App Manifest for AcadMind PWA with name, short_name, description, standalone display mode, teal theme color, and 192×192 / 512×512 icon entries pointing to the generated AcadMind cartoon logo assets.",
  "frontend/public/sw.js": "Service Worker implementing a cache-first offline strategy — pre-caches static assets on install, serves cached responses immediately with background revalidation on fetch, falls back to index.html for offline navigation, and cleans up stale caches on activate.",
  "frontend/index.html": "HTML entry point for AcadMind with manifest link, theme-color meta, iOS PWA meta tags, a `<link rel=\"icon\">` favicon pointing to the AcadMind cartoon logo at `/assets/generated/icon-192x192.dim_192x192.png`, and an apple-touch-icon using the same asset.",
  "frontend/src/components/InstallBanner.tsx": "Dismissible PWA install banner that captures the beforeinstallprompt event, shows an Install/Dismiss UI at the bottom of the screen, triggers the native install prompt on click, and persists dismissal to localStorage under 'acadmind_pwa_dismissed'.",
  "frontend/src/components/ThemeToggle.tsx": "New component that renders a sun/moon icon button using `useTheme` from `next-themes` to toggle between light and dark modes, with a tooltip indicating the current action.",
  "frontend/src/pages/Guide.tsx": "New comprehensive Guide page at /guide with six module sections (Dashboard, AI Chat, OCR Timetable, Manual Timetable, Assignments, Day Tracker), each rendered as a card with icon, description, numbered steps, and tips. The AI Chat section includes the Google AI Studio API Keys screenshot, a prominent clickable link to https://aistudio.google.com/api-keys, and a quick-nav pill bar at the top for jumping between sections.",
  "frontend/src/components/UserProfileModal.tsx": "New modal dialog for setting up or editing the user profile (name/email and Gemini API key), with sign-out functionality; used in the Sidebar footer."
}