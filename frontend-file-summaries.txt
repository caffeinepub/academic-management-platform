{
  "frontend/src/index.css": "Global styles with OKLCH teal/slate-green color system for light and dark modes, custom font imports (Inter + Plus Jakarta Sans), and scrollbar utilities.",
  "frontend/tailwind.config.js": "Extended Tailwind config with teal/slate-green palette, Inter/Plus Jakarta Sans fonts, custom border radii, card shadows, and typing animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations (slots, tasks, chat messages) plus fully localStorage-based Day Tracker hooks (useGetDayEntries, useAddDayEntry, useUpdateDayEntry, useDeleteDayEntry) using the new LocalDayEntry schema with id, date, hours, description, and completed fields, including migration logic for old entries.",
  "frontend/src/utils/mockOcr.ts": "Client-side mock OCR utility that simulates timetable extraction from an image file with a delay, returning randomized slot data.",
  "frontend/src/utils/mockAi.ts": "Client-side mock AI response generator that produces context-aware replies referencing stored timetable slots and tasks.",
  "frontend/src/App.tsx": "Main app component with routing, ThemeProvider, UserProvider, a ServiceWorkerRegistrar effect (production-only), the InstallBanner component, and the Toaster — all wired together at the root level.",
  "frontend/src/components/Layout.tsx": "Shell layout with collapsible sidebar, mobile hamburger header (now including ThemeToggle on the right), a desktop absolute-positioned ThemeToggle in the top-right corner, main content area, and updated footer with caffeine.ai attribution.",
  "frontend/src/components/Sidebar.tsx": "Sidebar navigation with all existing nav links plus a new \"Gemini API Key\" section at the bottom — shows a password input + Save button when no key is stored, and a masked key display + Remove button when a key exists, with toast feedback.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard with hero banner, stats, four quick-action buttons, today's schedule card, and upcoming tasks card fetched from backend.",
  "frontend/src/pages/Chat.tsx": "AI chat page that calls Gemini 2.5 Flash directly via `callGeminiApi` (no ICP canister calls for chat), stores all messages in local React state only, shows an amber alert when no API key is present, and maintains full conversation history, typing indicator, clear chat, and Enter-to-send functionality.",
  "frontend/src/components/TimetableSlotModal.tsx": "Reusable dialog for adding/editing timetable slots with subject, day, time, duration, and location fields with validation.",
  "frontend/src/components/TaskModal.tsx": "Reusable dialog for adding/editing tasks with title, due date, and description fields with validation.",
  "frontend/src/pages/TimetableManual.tsx": "Weekly grid timetable page with day columns, slot cards with hover edit/delete controls, and add/edit/delete modal with toast feedback.",
  "frontend/src/pages/TimetableUpload.tsx": "OCR upload page with drag-and-drop image upload, preview, and extract button — clicking \"Extract Timetable\" now shows a brief loading spinner followed by a styled \"Coming Soon\" panel instead of running mock OCR, while preserving all other upload and slot-editing functionality.",
  "frontend/src/pages/Assignments.tsx": "Assignments tracker page with filter tabs (all/pending/completed), task cards with complete toggle, overdue/urgent badges, and add/edit modal — updated to import `useMarkTaskCompleted` (matching the renamed hook in useQueries.ts).",
  "frontend/src/lib/utils.ts": "Utility function cn() combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/DayTracker.tsx": "Day Tracker page with always-visible Edit (pencil) and Delete (trash) buttons on every entry card, an EntryModal dialog pre-populated for editing, a delete confirmation AlertDialog, inline loading states on buttons, and a DailySummary section that recalculates totals after any change — all backed by localStorage via React Query hooks.",
  "frontend/src/contexts/UserContext.tsx": "New React context that manages the user's Gemini API key, persisting it to localStorage. Exposes `useUser` hook with `geminiApiKey`, `updateGeminiApiKey`, and `clearGeminiApiKey`.",
  "frontend/src/utils/geminiChat.ts": "Utility that builds a dynamic system prompt from the user's timetable slots and tasks, then calls the Gemini 2.5 Flash REST API with conversation history and returns the AI response text.",
  "frontend/public/manifest.json": "Web App Manifest for AcadMind PWA with name, short_name, description, standalone display mode, teal theme color (#0d9488), and 192×192 / 512×512 icon entries pointing to the generated assets.",
  "frontend/public/sw.js": "Service Worker implementing a cache-first offline strategy — pre-caches static assets on install, serves cached responses immediately with background revalidation on fetch, falls back to index.html for offline navigation, and cleans up stale caches on activate.",
  "frontend/index.html": "HTML entry point updated with manifest link, theme-color meta, iOS home-screen meta tags (apple-mobile-web-app-capable, status-bar-style, title), and apple-touch-icon, plus updated page title to \"AcadMind\".",
  "frontend/src/components/InstallBanner.tsx": "Dismissible PWA install banner that captures the beforeinstallprompt event, shows an Install/Dismiss UI at the bottom of the screen, triggers the native install prompt on click, and persists dismissal to localStorage under 'acadmind_pwa_dismissed'.",
  "frontend/src/components/ThemeToggle.tsx": "New component that renders a sun/moon icon button using `useTheme` from `next-themes` to toggle between light and dark modes, with a tooltip indicating the current action."
}